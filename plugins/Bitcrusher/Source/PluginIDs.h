#pragma once

namespace PluginIDs
{
    // Parameter IDs (must match APVTS layout)
    inline constexpr const char* bits = "bits";
    inline constexpr const char* rate = "rate";
    inline constexpr const char* mix = "mix";
}
