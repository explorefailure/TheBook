# Startup

Context-aware session greeting. Shows banner, last checkpoint, and uncommitted changes.

Execute the startup skill directly - display the banner, find last checkpoint, check git status, and show context-aware output.
